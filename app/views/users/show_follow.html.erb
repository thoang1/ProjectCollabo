<% provide(:title, @title) %>

    <section>
      
      <h1><%= @user.full_name %></h1>
      <span><%= link_to "view my profile", @user %></span>
      
    </section>
    <section>
      <%= render 'shared/follow_stats' %>
    </section>
  </aside>
  <div class="">
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <% @users.each do |user| %>
    <li><%= link_to user.full_name, user_path(user) %> 
    <%= user.gender %> <div id="follow_form">
  <% if current_user.following?(uuser) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div></li>

  <% end %>
    <% end %>
  </div>
